// auth-config.ts (raíz) — Referencia redirigida a backend/auth.config.ts
// Este archivo es un alias para compatibilidad con importaciones antiguas.
// La implementación real está en backend/auth.config.ts
export { authConfig, requireAuth, requireRole } from './backend/auth.config.ts';
